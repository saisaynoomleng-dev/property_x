import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Post Free Property',
};

const PostFreePage = () => {
    return <div>PostFreePage</div>;
};

export default PostFreePage;
