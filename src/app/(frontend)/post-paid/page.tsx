import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Post Paid Property',
};

const PostPaidPage = () => {
    return <div>PostPaidPage</div>;
};

export default PostPaidPage;
